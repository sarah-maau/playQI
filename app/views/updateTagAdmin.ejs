<%- include('partials/header') %>

<div class="pricing-header px-3 py-3 pt-md-5 pb-md-4 mx-auto text-center">
    <h2 class="h1 display-4">Modifier la catégorie : <%=tag.name%></h2>
</div>

<div class="row">
    <div class="col-6 offset-3">
        <form action="/admin/tag/<%=tag.id%>" method="post" class="form-signup">
            <%- include('partials/error') %>
            <div class="form-label-group">
                <input type="text" class="form-control" id="tagName" name="tagName" aria-describedby="tagNameHelp"
                    placeholder="Nouveau nom">
                <label for="tagName">Nouveau nom</label>
            </div>
            <button class="btn btn-lg btn-primary btn-block" type="submit">Modifier</button>
        </form>
    </div>
</div>

<%- include('partials/footer') %> 